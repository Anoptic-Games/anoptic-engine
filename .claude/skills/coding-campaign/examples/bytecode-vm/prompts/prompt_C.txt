Implement a single Rust function: a semantics-preserving peephole optimizer for a stack-machine bytecode.

The instruction type is ALREADY DEFINED elsewhere in the crate as the following enum. Do NOT redefine it, do NOT write `enum Op`, do NOT write any `use` for it. Refer to `Op` and its variants directly:

    enum Op { Push(i64), Pop, Add, Sub, Mul, Div, Mod, Neg, Dup, Swap, Jmp(usize), Jz(usize), Print, Halt }

Define exactly (it MUST be `pub`):

    pub fn optimize(program: &[Op]) -> Vec<Op>

Apply safe optimizations that make the program shorter without changing its observable behavior, for example:
- Constant folding of arithmetic over adjacent Push constants: Push(a),Push(b),Add -> Push(a+b); likewise Sub/Mul/Div/Mod; and Push(a),Neg -> Push(-a). Do NOT fold a Div or Mod whose divisor is 0; leave those unchanged. Prefer wrapping arithmetic when folding so you never panic.
- Push(x),Pop -> (remove both).
- Any other rewrite you can prove is sound.

CRITICAL: `Jmp(index)` and `Jz(index)` are ABSOLUTE instruction indices. Whenever you remove or insert instructions, rewrite every jump target so it still points at the correct instruction. Stale jump targets = WRONG. `run(optimize(p))` must equal `run(p)` for every program. When in doubt, leave a region unchanged.

Output ONLY the Rust source for `pub fn optimize` plus any private helper items it needs. No `Op` definition. No `main`. No tests. No external crates (std only). No markdown code fences. No prose before or after.
